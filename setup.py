from setuptools import setup

setup(setup_requires=['visdom', 'torch', 'torchvision', 'jupyter', 'matplotlib', 'transforms3d'],
      install_requires=['visdom', 'torch', 'torchvision', 'jupyter', 'matplotlib', 'transforms3d'])
